index.php
主页面，包含卡片式日记列表、搜索、统计和主题切换
add.php
新增日记表单
edit.php
编辑日记表单（通过 Modal + DB 获取内容）
delete.php
删除功能（简单 PHP 删除）
export.php
日记列表 + 导出为 PDF 链接
db.php
基本数据库操作（支持 fetch）
tcpdf/
接口使用 TCPDF 进行 PDF 生成（如支持，需手动引用）
前端资源
包括 Bootstrap 和 Font Awesome 的 CDN 引用（可选本地化）

暗色 / 亮色切换按钮
表格形式为 HTML 卡片（样式美化）
AJAX 内容加载（通过 fetch 和 JSON 支持）
数据统计（日记总数、日期信息）
无需刷新的 Modal 编辑与删除
搜索功能（实时过滤）
完整功能架构
部分基于 Bootstrap 5 的现代化UI

QWEN AI 生成
